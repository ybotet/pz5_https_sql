syntax = "proto3";

package auth;

option go_package = "github.com/ybotet/pz5_https_sql/gen/proto/auth";

service AuthService {
  rpc Verify(VerifyRequest) returns (VerifyResponse);
}

message VerifyRequest {
  string token = 1;
}

message VerifyResponse {
  bool valid = 1;
  string subject = 2;
}